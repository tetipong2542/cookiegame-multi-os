# CookieGame — Python dependencies
# ติดตั้งด้วย:  pip install -r requirements.txt
#
# Python 3.12 (ต้นฉบับใช้ 3.12 — ใช้ 3.11+ ก็ได้)

# Computer vision (ตรงข้าม template ในหน้าจอเกม)
opencv-python>=4.9.0
numpy>=1.26.0

# License system (ECDSA signature verification)
ecdsa>=0.19.0

# Emergency-stop hotkey ('q' key global hook)
# หมายเหตุ Mac: ต้องรันด้วย sudo หรือให้สิทธิ์ Accessibility ใน System Settings
keyboard>=0.13.5

# GUI ใช้ tkinter ที่มากับ Python อยู่แล้ว
# ถ้าติดตั้ง Python ผ่าน brew ต้องลง tkinter เพิ่ม:
#   brew install python-tk@3.12
